# example environment for frontend
# URL по которому доступен backend API (через прокси или публичный адрес)
VITE_API_BASE_URL="http://localhost:3000/api"
# Порт, на котором будет запущен vite dev server
VITE_PORT=5173
# Продакшн-сборка: используется как переменная при резолве API
VITE_BASE_URL="/"
